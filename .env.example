# Required runtime keys
JINA_API_KEY=
OPENROUTER_API_KEY=

# Reverse-proxy site address. For automatic HTTPS, add a public DNS hostname after a comma.
# Example: SITE_ADDRESS="http://:80, app.example.com"
SITE_ADDRESS=http://:80

# Optional model and service overrides
LLM_MODEL=qwen/qwen3-8b
DEMO_ACCESS_CODE=
DEMO_MIN_REQUEST_INTERVAL=3
DEMO_MAX_QUESTIONS=20
EMBEDDING_MODEL=jina-embeddings-v3
EMBEDDING_QUERY_TASK=retrieval.query
EMBEDDING_DOCUMENT_TASK=retrieval.passage
RERANKER_MODEL=jina-reranker-v2-base-multilingual
REQUEST_TIMEOUT=90

# Query transformation is disabled by default to reduce latency.
RAG_QUERY_TRANSFORMATION=false

# Keep the latest six user/assistant turns, capped at 6000 characters.
RAG_MEMORY_MAX_TURNS=6
RAG_MEMORY_MAX_CHARS=6000
